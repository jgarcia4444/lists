

const initialState = {
    test: "subject",
}

const ListCreationReducer = (state=initialState, action) => {
    switch(action.type) {
        default:
            return state
    }
};

export default ListCreationReducer;